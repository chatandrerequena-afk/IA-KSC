# --- Núcleo obligatorio (sin esto la app no arranca) ---
streamlit>=1.38
pandas
requests
Pillow
groq

# --- Voz realista gratis (Edge TTS para hablar) ---
# Si falta, la app sigue funcionando pero sin audio de voz.
edge-tts>=6.1.9

# --- Arena Push-Up con esqueleto en cámara (opcional) ---
# Si falta, esa pestaña muestra un registro manual en vez de la cámara en vivo.
streamlit-webrtc
mediapipe
av
opencv-python-headless
aiortc
